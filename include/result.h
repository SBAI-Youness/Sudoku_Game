#pragma once

#include "./config.h"

// Function used to handle the result page rendering and input
extern void HandleResultProcess(enum GAME_STATE *game_state, enum GAME_DIFFICULTY *game_difficulty); 